<br />
<div align="center">
  <img src="https://static.sismo.io/readme/top-main.png" alt="Logo" width="150" height="150" style="borderRadius: 20px">

  <h3 align="center">
    Prove with Sismo (PwS)
  </h3>

  <p align="center">
    Made by <a href="https://docs.sismo.io/" target="_blank">Sismo</a>
  </p>
  
  <p align="center">
    <a href="https://discord.gg/sismo" target="_blank">
        <img src="https://img.shields.io/badge/Discord-7289DA?style=for-the-badge&logo=discord&logoColor=white"/>
    </a>
    <a href="https://twitter.com/sismo_eth" target="_blank">
        <img src="https://img.shields.io/badge/Twitter-1DA1F2?style=for-the-badge&logo=twitter&logoColor=white"/>
    </a>
  </p>
</div>

[Prove with Sismo presentation]

## Installation

```
$ yarn add @sismo-core/pws
$ npm i @sismo-core/pws
```

## Usage

**Frontend**
```javascript
import { PwSButton, usePws } from "@sismo-core/pws";
import { DisplayGroup } from "..";

const REQUEST = `[Group id]`;
  
const Page = () => {
    const { proof } = usePws();
    const [group, setGroup] = useState();

    useEffect(() => {
        if (!proof) return;
        const verifyProof = async () => {
            const isVerified = await sendToBackend(proof);
            if (isVerified) {
                navigateTo('/gated-service')
            } 
        }
        verifyProof();
    }, [proof])

    useEffect(() => {
      const getData = async () => {
        const res = await getGroup({ groupId: "[Group id]" })
        setGroup(res);
      }
      getData();
    }, [])

    return <div>
        <DisplayGroup group={group} />
        <PwSButton appId="[Your app id]" request={REQUEST}/>
    </div>
}
```

**Backend**
```javascript
import { PwSVerifier } from "@sismo-core/pws";
import { isInDatabase, storeInDatabase } from "..";

const REQUEST = `[Group id]`;

const isVerified = (proof: Proof, claims: Claim[]) => {
  const pws = new PwSVerifier({ appId: "[Your app id]" });
  const verifiedClaims: VerifiedClaim[] = await pws.verify(REQUEST, { proof, claims });
  if (isInDatabase(verifiedClaims[0].nullifier)) return false;
  storeInDatabase(verifiedClaims[0].nullifier);
  return true;
}
```

## Documentation


### PwSButton (React.js)

```javascript
const REQUEST = `[Group id]`

<div>
    <PwSButton appId="[Your app id]" request={REQUEST}/>
</div>
```

| Params | Type | Description |
|---|---|---|
| appId | String | Identifier of your app |
| request | String | Request sent to Prove with Sismo | 

**Optional params**

```javascript
<div>
    <PwSButton 
      appId="[Your app id]" 
      callbackPath="/success" 
      serviceId="" 
      request={REQUEST}
      acceptHigherValues={false}
    />
</div>  
```

| Params | Default | Type | Description |
|---|---|---|---|
| callbackPath | null | String | Path of the page where you want to redirect the user after the proof has been generated |
| serviceId | main | string | Service ID of the proof. It defines the scope of the user's nullifier |
| acceptHigherValues | true | boolean | Define whether or not the user can decrease their value |

### usePws (React.js)

Include the usePws in your success page to retrieve the proof generated by the user.

```javascript
const { claims, proof, error } = usePws();
```

| | Type | Description |
|---|---|---|
| proof | Proof | Proof generated by your user |
| request | String | Corresponding Proof request |
| error | Error | Error returned by the Sismo app if the user fails to generate their proof |

```javascript
type Proof = {
  request: String;
  content: string;
}

type Claim = {
  appId: string;
  serviceId: string;
  request: string;
  timestamp: "latest" | number;
  acceptHigherValues: boolean;
  isStrict: boolean;
  value: "MAX" | number;
}

type Error = {
  message: string
}
```

### prove (prefer using PwSButton)

```javascript
import { prove } from "@sismo-core/pws";

const REQUEST = `[Group id]`;
  
const Page = () => {
    const submit = () => {
      prove({
        appId: "[Your app id]",
        request: REQUEST
      })
    }

    return <div>
        <button onClick={() => submit()}/>
    </div>
}
```

### PwSVerifier

**Initialization** 
```javascript
const pws = new PwSVerifier({ appId: "[Your app id]" });
```

| Params | Type | Description |
|---|---|---|
| appId | Identifier of your app |

**verify** 
```javascript
const REQUEST = `[Group id]`;

const verifiedClaims: VerifiedClaim[] = await pws.verify(REQUEST, { proof, claims });
```

| Params | Type | Description |
|---|---|---|
| proof | Proof | Proof generated by the user |
| claims | Claim[] | Claims sent back from Prove with Sismo |

If the proof is valid, the function should return a VerifiedClaim[], otherwise, it should return an error.

```javascript
type Proof = {
  request: Request;
  content: string;
}

type Claim = {
  appId: string;
  serviceId: string;
  request: string;
  timestamp: "latest" | number;
  acceptHigherValues: boolean;
  isStrict: boolean;
  value: "MAX" | number;
}

type VerifiedClaim = { 
  nullifier: string;
  value: number;
  groupName: string;
  timestamp: number;
  isStrict: boolean;
}

type Error = { 
  isValid: false;
  message: string;
}
```

**Optional params** 

```javascript
const verifiedClaims = await pws.verify({ proof: proof, serviceId: "[Service id]" });
```

| Params | Type | Description |
|---|---|---|
| serviceId | String | Service ID of the proof. It defines the scope of the user's nullifier. The serviceId must match the serviceId sent to Prove with Sismo|

### getGroup

```javascript
const group = await getGroup({ groupId: "[Group id]", timestamp: "" })
```

| Params | Type | Description |
|---|---|---|
| groupId | String | Group Identifier |
| timestamp | String | Timestamp of the snapshot you want to fetch | 

## License

Distributed under the MIT License.

## Contribute

Please, feel free to open issues, PRs or simply provide feedback!

## Contact

Prefer [Discord](https://discord.gg/sismo) or [Twitter](https://twitter.com/sismo_eth)

<br/>
<img src="https://static.sismo.io/readme/bottom-main.png" alt="bottom" width="100%" >
